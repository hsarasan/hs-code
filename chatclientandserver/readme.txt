This is a very simple broadcast chat client & server 

Functionality

  Broadcasts message from a client to every other client

Server

 There is a single chatserver running which handles connection with clients and broadcasting messages

Client

  Connects to the server and sends broadcast messages to server to propogate to all other clients
